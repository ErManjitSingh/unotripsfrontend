/** Destination detail uses full Veena-style template (own chrome + footer) inside the page. */
export default function DestinationsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
