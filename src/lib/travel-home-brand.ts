import { SITE } from "@/lib/constants";

/** Travel home — EaseMyTrip-style layout with UNO Trips branding. */
export const TRAVEL_HOME_BRAND = {
  name: SITE.name,
  tagline: "Bharat ka Travel App",
} as const;

/** Home hero navbar wordmark — `public/images/homelogo.webp` */
export const TRAVEL_HOME_LOGO_SRC = "/images/homelogo.webp" as const;
