# Laravel API base (no trailing slash). Leave empty to use built-in mock data.
NEXT_PUBLIC_API_URL=

# Canonical site URL for metadata, Open Graph, and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://www.wanderluxvoyages.com
